apiVersion: v1
kind: Pod
metadata:
  name: deb-req-high
spec:
  containers:
  - name: deb
    image: debian
    command:
      - /bin/sleep
    args:
      - "1000000"
    resources:
      requests:
        cpu: 1
        memory: 1000Mi
    volumeMounts:
    - name: apt
      mountPath: /etc/apt/sources.list
      subPath: sources.list
      readOnly: true
    - name: apt
      mountPath: /etc/apt/apt.conf.d/98proxy
      subPath: 98proxy
      readOnly: true
  volumes:
  - name: apt
    configMap:
      name: apt
